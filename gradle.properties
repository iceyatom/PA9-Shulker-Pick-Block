# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties — confirmed versions for Minecraft 26.3 (see CLAUDE.md / README.md)
#   check these on https://fabricmc.net/develop / https://meta.fabricmc.net
minecraft_version=26.3
# NOTE: Yarn mappings are NOT published for 26.x at the time of writing (see CLAUDE.md
# "Build Environment Findings"). Left here for when/if a Yarn build is released.
loader_version=0.19.5

# Mod Properties
mod_version=1.1.0
maven_group=com.yourname.shulkerpickblock
archives_base_name=shulker-pick-block

# Dependencies
fabric_version=0.160.6+26.3
# Mod Menu — compile-only, for the config screen entrypoint (FR-25). Must be a 26.3-compatible
# build; 21.0.0-beta.1 is the first Mod Menu release for 26.3.
mod_menu_version=21.0.0-beta.1
